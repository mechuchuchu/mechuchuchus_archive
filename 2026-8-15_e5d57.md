작은 모델들의 집단 지능: Gradient 통신 없이도 가능한가?

1. 문제의식

대규모 모델 학습에서는 모델의 파라미터와 gradient를 수많은 GPU 사이에서 지속적으로 동기화해야 한다. 모델이 커질수록 계산량뿐 아니라 gradient communication이 중요한 병목이 된다.

여기서 다음과 같은 질문을 던져볼 수 있다.

> 각각의 작은 모델이 독립적으로 학습되고, 학습 과정에서는 gradient를 전혀 공유하지 않은 채, 마지막에 모델의 출력(logits)만 결합한다면 하나의 큰 모델에 가까운 능력을 얻을 수 있을까?



이를 확인하기 위해 MNIST에서 극단적으로 작은 bottleneck 모델을 사용한 toy experiment를 수행했다.


---

2. 기본 모델

모든 teacher는 다음과 같은 구조를 사용했다.

\[
784 \rightarrow 5 \rightarrow 10
\]

즉 MNIST의 784차원 입력을 단 5차원 hidden representation으로 압축한 뒤 10개 digit class를 예측한다.

이는 일반적인 MNIST classifier에 비해 매우 강한 정보 병목이다.

각 모델의 logits를

\[
z_i(x)\in\mathbb{R}^{10}
\]

이라고 하면 ensemble은 단순히

\[
z_{\mathrm{ens}}(x)
=
\frac{1}{N}
\sum_{i=1}^{N}z_i(x)
\]

로 계산했다.


---

3. 실험 1 — 독립적으로 학습한 5개 모델의 ensemble

먼저 5개의 \(784\rightarrow5\rightarrow10\) 모델을 각각 독립적으로 학습했다.

각 모델은 동일한 MNIST 전체 training set을 사용했지만 서로 다른 초기화로 학습했다.

개별 모델의 정확도는 약 27.9~89.3% 범위였고 평균은 약 50% 수준이었다.

그런데 5개 모델의 logits를 ensemble하자:

\[
\boxed{83.13\%}
\]

의 정확도를 얻었다.

이는 개별 모델 평균인 약 50%보다 훨씬 높다.

결과

방법	Accuracy

개별 teacher 평균	50.01%
Logit ensemble	83.13%


이는 개별 모델의 오류가 서로 완전히 동일하지 않기 때문에 ensemble을 통해 상쇄될 수 있음을 보여준다.


---

4. 실험 2 — Ensemble Knowledge Distillation

다음으로 5개 teacher의 logits를 16차원 hidden layer를 가진 student에게 증류했다.

Student 구조:

\[
784\rightarrow16\rightarrow10
\]

teacher ensemble의 logits을 soft target으로 사용했다.

초기 실험에서는 hard-label loss도 함께 사용했으며:

\[
L =
0.8L_{\mathrm{KD}}
+
0.2L_{\mathrm{CE}}
\]

결과:

\[
\boxed{89.40\%}
\]

를 얻었다.

이후 hard-label loss를 제거하고 teacher logits만으로 KD를 수행했다.

그 결과:

\[
\boxed{82.57\%}
\]

가 나왔다.

이는 ensemble의 정보만으로도 상당한 성능을 student에게 전달할 수 있음을 보여준다.


---

5. 실험 3 — Ensemble 자체의 성능

KD student와 ensemble 자체를 비교했다.

결과는:

\[
\text{Ensemble}=83.13\%
\]

\[
\text{KD Student}=82.57\%
\]

였다.

즉 student가 단순히 teacher ensemble의 출력을 상당히 잘 압축하고 있었다.

이 결과는 ensemble → distillation이라는 구조가 작은 모델 집단의 지식을 더 큰 단일 모델로 전달하는 방법으로 작동할 수 있음을 보여준다.


---

6. 실험 4 — 공동 Gradient Training

다음으로 더욱 흥미로운 실험을 수행했다.

3개의 \(784\rightarrow5\rightarrow10\) 모델을 동시에 학습하면서 각각의 logits를 평균했다.

\[
z_E=
\frac{z_1+z_2+z_3}{3}
\]

그리고 ensemble 결과에 대해서만 loss를 계산했다.

\[
L=CE(z_E,y)
\]

이 loss를 역전파하면 세 모델 모두 gradient를 받는다.

놀랍게도 최종 결과는:

모델	Accuracy

Model 1	36.28%
Model 2	26.37%
Model 3	38.46%
개별 평균	33.70%
Ensemble	93.08%


였다.

즉 개별 모델의 성능은 오히려 낮았지만 ensemble 성능은 크게 증가했다.

\[
\boxed{33.70\%\rightarrow93.08\%}
\]

이는 중요한 현상을 보여준다.

> 개별 모델을 강하게 만드는 것과 ensemble 전체를 강하게 만드는 것은 서로 다른 optimization objective일 수 있다.



각 모델이 독립적으로 정답을 맞힐 필요 없이, 세 모델의 logits가 합쳐졌을 때만 정답이 되도록 specialization할 수 있기 때문이다.


---

7. 실험 5 — Gradient 통신 없는 Data-Shard 학습

마지막으로 처음의 문제의식에 가장 가까운 실험을 수행했다.

MNIST 60,000개를 세 개의 disjoint shard로 나누었다.

\[
60,000
\rightarrow
20,000+20,000+20,000
\]

각 모델은 자신의 20,000개 데이터만 사용했다.

즉:

다른 모델의 training data를 보지 않음

gradient 공유 없음

parameter 공유 없음

학습 중 communication 없음


학습이 끝난 뒤 logits만 ensemble했다.

결과:

모델	Accuracy

Model 1	65.00%
Model 2	66.19%
Model 3	67.45%
개별 평균	66.21%
Logit ensemble	81.36%


개별 평균에서:

\[
66.21\%\rightarrow81.36\%
\]

라는 상당한 ensemble gain이 발생했다.

특히 중요한 점은 각 모델이 전체 데이터를 보지 않았음에도 ensemble이 **81.36%**까지 올라갔다는 것이다.


---

8. 현재까지의 결과

전체 실험을 요약하면 다음과 같다.

Experiment	Individual	Ensemble / Student

5 independent full-data teachers	50.01%	83.13%
KD student, ensemble-only	—	82.57%
3 jointly trained models	33.70%	93.08%
3 independent data-shard models	66.21%	81.36%


가장 중요한 관찰은 세 가지다.

① 약한 모델도 ensemble하면 강해질 수 있다

개별 모델의 정확도가 낮더라도 모델 간 오류가 충분히 다르면 ensemble을 통해 상당한 성능 향상이 가능했다.

② logits에는 최종 prediction보다 많은 정보가 있다

개별 teacher의 accuracy만 보면 약한 모델들이었지만, logits를 이용한 distillation에서는 훨씬 높은 성능을 얻을 수 있었다.

③ Gradient 통신 없이도 일부 능력을 결합할 수 있다

서로 다른 데이터 shard를 독립적으로 학습한 모델들의 logits만 결합했을 때도:

\[
66.21\%\rightarrow81.36\%
\]

의 성능 향상이 관찰됐다.

이는 distributed independent training → output aggregation이라는 구조의 가능성을 보여주는 작은 실험적 근거가 된다.


---

9. 하지만 MNIST 결과를 과대해석해서는 안 된다

현재 결과는 매우 흥미롭지만, MNIST는 지나치게 작은 문제다.

특히 다음 요소들이 아직 검증되지 않았다.

더 복잡한 입력 표현

더 많은 class

convolutional representation

대규모 language modeling

모델 간 knowledge diversity

parameter scaling

communication/computation trade-off

데이터가 완전히 다른 경우의 transfer

ensemble 결과를 다시 단일 모델로 압축하는 과정에서의 정보 손실


따라서 다음 단계에서는 훨씬 복잡한 benchmark로 확장할 필요가 있다.


---

10. 다음 단계: CIFAR-10

첫 번째 확장은 CIFAR-10이다.

MNIST에서는 입력이 단순한 grayscale digit이었지만 CIFAR-10에서는 RGB 이미지와 훨씬 복잡한 visual representation을 다룬다.

실험 구조는 다음과 같이 확장할 수 있다.

\[
\text{CIFAR-10}
\rightarrow
\text{small CNN/ViT}
\]

여러 개의 작은 모델을 독립적으로 학습시키고,

\[
z_{\mathrm{ens}}
=
\frac{1}{N}
\sum_i z_i
\]

로 logits를 결합한다.

여기서는 특히 모델 수와 ensemble 성능의 관계를 측정할 예정이다.

\[
N=1,2,4,8,16,\ldots
\]

그리고 동시에 communication cost를 측정한다.


---

11. 그 다음: ImageNet

CIFAR-10에서 현상이 유지된다면 다음 단계는 ImageNet이다.

ImageNet에서는 모델의 표현 능력과 데이터 다양성이 훨씬 커지기 때문에, MNIST에서 관찰된 단순한 ensemble 효과와 실제 large-scale distributed learning 사이의 차이를 볼 수 있다.

특히 다음 세 가지를 비교할 수 있다.

Centralized training

\[
M_{\mathrm{large}}
\leftarrow
D_{\mathrm{all}}
\]

Independent ensemble

\[
M_1\leftarrow D_1,\quad
M_2\leftarrow D_2,\quad
\ldots
\]

그리고 마지막에:

\[
\{M_i\}
\rightarrow
\text{logit aggregation}
\]

Distillation

\[
\{M_i\}
\rightarrow
z_{\mathrm{ensemble}}
\rightarrow
M_{\mathrm{student}}
\]

이 세 가지를 동일한 compute budget에서 비교할 수 있다.


---

12. 최종적으로는 GPT-2로 확장

가장 흥미로운 단계는 language model이다.

이미지 classification에서는 logits가 10개 또는 1000개 class에 대한 정보지만, language model에서는 매 token마다 vocabulary 전체에 대한 probability distribution이 존재한다.

즉:

\[
z_i(x_t)\in\mathbb{R}^{|V|}
\]

이고 ensemble은:

\[
z_E(x_t)
=
\frac{1}{N}
\sum_i z_i(x_t)
\]

가 된다.

이를 이용해 여러 개의 작은 GPT-2 계열 모델을 독립적으로 학습하고, 최종적으로 logits를 aggregation하거나 distillation한다.

여기서 진짜 중요한 질문이 생긴다.

> 거대한 모델 하나를 gradient synchronization하면서 학습하는 것과, 수많은 작은 모델을 서로 독립적으로 학습한 뒤 output-level에서 결합하는 것 사이에 어떤 scaling law가 존재하는가?




---

13. 궁극적인 가설

이번 MNIST 실험에서 관찰된 현상을 일반화하면 다음과 같은 가설을 세울 수 있다.

\[
\boxed{
\text{Many weak independent models}
+
\text{aggregation}
\approx
\text{one stronger model}
}
\]

그리고 더 나아가:

\[
\boxed{
\text{Compute}
\rightarrow
\text{independent learners}
\rightarrow
\text{small communication}
\rightarrow
\text{ensemble/distillation}
}
\]

이라는 새로운 distributed training paradigm을 생각해볼 수 있다.

기존의 대규모 학습이

\[
\text{GPU}
\leftrightarrow
\text{GPU}
\leftrightarrow
\text{GPU}
\]

사이에서 지속적으로 gradient를 교환하는 구조라면, 이 접근은 극단적으로:

\[
\text{GPU}_1
\quad
\text{GPU}_2
\quad
\text{GPU}_3
\quad\cdots
\]

가 완전히 독립적으로 학습한 뒤,

\[
\boxed{\text{마지막에 output만 교환}}
\]

하는 구조를 목표로 한다.

MNIST 실험은 이 아이디어가 적어도 작은 문제에서는 단순한 이론적 가능성에 그치지 않고 실제 성능 향상을 만들어낼 수 있음을 보여주었다.


---

14. 앞으로 검증할 핵심 질문

다음 실험에서는 단순 정확도보다 다음 질문들을 중심으로 평가할 예정이다.

1. 모델 수가 증가하면 ensemble 성능은 어떻게 scaling되는가?


2. 각 모델의 크기를 줄이고 모델 수를 늘리는 것이 유리한가?


3. 독립 학습과 joint training 사이의 차이는 얼마나 큰가?


4. 데이터를 shard하면 성능 손실은 얼마나 발생하는가?


5. logits만 교환했을 때 필요한 communication량은 얼마인가?


6. ensemble을 다시 하나의 student model로 distill하면 얼마나 압축할 수 있는가?


7. 이 현상이 CIFAR-10에서도 유지되는가?


8. ImageNet에서도 유지되는가?


9. language model에서도 같은 현상이 나타나는가?


10. 최종적으로 GPT-2 규모에서 compute 대비 communication 효율이 기존 distributed training보다 좋아질 수 있는가?



특히 마지막 질문이 핵심이다.

MNIST에서 81~93%가 나왔다는 사실 자체보다, 모델을 작게 쪼갰을 때 감소하는 계산 효율과 gradient communication 절감 사이에 어떤 최적점이 존재하는지가 이 연구의 진짜 관심사다.

다음 단계는 CIFAR-10 → ImageNet → GPT-2 순으로 문제의 복잡도를 높이면서 이 가설이 어디까지 유지되는지를 확인하는 것이다.
